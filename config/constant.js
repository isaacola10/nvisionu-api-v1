const EventStatuses = {
  active: "Active",
  closed: "Closed"
}

const PaymentStatuses = {
  successful: "Successful",
  pending: "Pending",
  failed: "Failed"
}

const RsvpStatuses = {
  active: "Active",
  used: "Used",
  pending: "Pending"
}

const CacheKeyTypes = {
  login: "Login"
}

const TokenTypes = {
  login: "Login"
}

const EventPay = {
  free: "free",
  charged: "charged"
}

module.exports = {
  EventStatuses,
  PaymentStatuses,
  RsvpStatuses,
  CacheKeyTypes,
  TokenTypes,
  EventPay,
}